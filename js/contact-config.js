/* ============================================================
   Yusei Stay — Contact details config
   Add only owner-confirmed public business details. Empty values
   remain visible as "pending" and are never turned into fake links.
   ============================================================ */

window.YuseiContactConfig = {
  whatsapp: "+81 80-1326-0364",
  email: "",
  phone: ""
};
